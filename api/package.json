{
  "name": "wordleish-api",
  "version": "1.0.0",
  "lockfileVersion": 2,
  "requires": true,
  "scripts": {
    "start": "node server.js",
    "test": "echo 'Error: no test specified' && exit 1",
    "format": "npx prettier --write ."
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.1",
    "prettier": "^2.7.1",
    "sqlite3": "^5.0.10"
  }
}
